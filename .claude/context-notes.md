<!-- 팀 에이전트 스캐폴딩 결정 기록 -->

# Context Notes — 팀 에이전트 스캐폴딩

## 결정
- **옵션 1 (Claude Code 서브에이전트)** 채택. SDK/MCP 대신 `.claude/agents/*.md`로 프로토타입.
- **11팀 단위** (34명 개인 단위 아님). 팀장 코드명 = 에이전트 id. 팀원 업무는 프롬프트 내 단계.
- **오케스트레이터 = 슬래시 커맨드** `/차수실행`. 이유: 서브에이전트끼리 호출 불가 →
  메인 스레드가 커맨드를 따라 각 팀을 순서대로 Task 호출하고 승인 게이트에서 정지.
- 승인 게이트 2곳은 AskUserQuestion으로 사람 결재. 승인 전 다음 단계 호출 금지 (사규 절대규칙 ⑦).

## 상태·도구 규약
- 모든 에이전트 마지막 줄 `[상태: 완료|반려|연동대기] — 사유`. 라이브 오피스 5종 상태와 매핑.
- NX·3D 챔버 = AI 미구동 → 입력 파일 없으면 `연동대기` 반환하고 정지 (사규 2-1).
- ADS·CST·엑셀 = 지금은 파일 읽어 구조화까지. 실구동은 team-tools MCP 붙인 뒤.

## 열려있는 것
- 실제 ADS/CST/엑셀 tool 연결 (MCP). 라이브 오피스 실시간 동기화.
